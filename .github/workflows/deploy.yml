# Deploy workflow scaffold (implemented in STEP 4).
name: Deploy

on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deploy workflow scaffolded in STEP 1."
