// Temporary landing page used to verify Docker infrastructure.
export default function HomePage() {
  return (
    <main style={{ minHeight: '100vh', display: 'grid', placeItems: 'center' }}>
      <section style={{ textAlign: 'center' }}>
        <h1 style={{ marginBottom: '0.5rem' }}>Calm Table</h1>
        <p style={{ margin: 0 }}>Infrastructure is running. Frontend implementation continues in STEP 5.</p>
      </section>
    </main>
  );
}
